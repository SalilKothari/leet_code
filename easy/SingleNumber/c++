class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int target = 0;
        
        for(const int &num: nums){
            target = target^num;
            
        }
        
        return target;
    }
};
