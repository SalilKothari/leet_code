class Solution {
public:
    int hammingDistance(int x, int y) {
        std::bitset<32> xBit(x);
        std::bitset<32> yBit(y);
        
        int diff = 0;
        for(int i = 0; i<32; ++i){
            if(xBit[i] != yBit[i]) ++diff;
        }
        
        return diff;
    }
};
