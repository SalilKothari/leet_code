class Solution:
    def hammingDistance(self, x: int, y: int) -> int:
        
        
        #try with bitshifting
        
        distance = 0
        
        for bitPos in range(32):
            distance += (x&1 != y&1)
            x >>= 1
            y >>= 1
        
        return distance

        
        
