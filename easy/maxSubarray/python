class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        
        best = nums[0]
        running = best
        
        for num in nums[1:]:
            
            running = max(running,0) + num
            
            best = max(running, best)
            
        return best
      
