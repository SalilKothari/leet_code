# First Try

WITH appearsOnce AS (
    SELECT num
    FROM MyNumbers
    GROUP BY num
    HAVING COUNT(*) = 1
)

SELECT MAX(num) AS num
FROM appearsOnce


# derived table:
SELECT MAX(NUM) as num
FROM (
    SELECT num, COUNT(*) as count
    FROM MyNumbers
    GROUP BY num
) y
WHERE count = 1;
